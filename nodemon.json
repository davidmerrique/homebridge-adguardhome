{
  "watch": [
    "src"
  ],
  "ext": "ts",
  "ignore": [],
  "exec": "clear && tsc && homebridge -U ./test/hbConfig -D",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}